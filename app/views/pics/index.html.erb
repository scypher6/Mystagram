

<div class="container">
    <h5 style="color:grey; margin-top:3%">Explore</h5>
    <div class="container">
    <div class="row">
    
    <!-- <% @pics.all.each do |pic|%>
        <div class="card">
            <img class="card-img-top" src=<%="#{pic.image.url}" %> alt="Card image cap">
            <%= link_to image_tag("#{pic.image.url}", alt:"#{pic.title}", width:"70%", height: "60%"), pic %> 
            <li><%= link_to pic.title, pic %></li>
        </div>
        <% end %>  -->
        
      <% sorted_pics = @pics.order('created_at DESC') %>
        <% sorted_pics.all.each do |pic|%>
            <div class="col-sm-4 h-100 py-2 px-2">
              <div class="card card-body h-100 mt-4" >
                <%= link_to image_tag("#{pic.image.url}", alt:"#{pic.title}", width:"100%", height: "100%"), pic %>
                <!-- <div><%= link_to pic.title, pic %></div> -->
                <div style="text-align: center;">
                    ❤ <%= pic.likes.count.to_i %>
                </div>
              </div>
            </div>
        <% end %> 
      </div>
    </div>
</div>






